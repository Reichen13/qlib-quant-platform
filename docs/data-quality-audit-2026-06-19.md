# Qlib 数据质量只读审计 - 2026-06-19

本审计只读取线上 `/home/ubuntu/.qlib/qlib_data/cn_data`，未删除、覆盖或更新任何数据。

## 已确认状态

- 交易日历最新日期：`2026-06-18`
- `features/*/close.day.bin` 文件数：`3885`
- 最新到 `2026-06-18` 的股票文件：`2861`
- 未到 `2026-06-18` 的股票文件：`1024`

全市场 close 文件最新日期主要分布：

| 最新日期 | 文件数 | 初步判断 |
| --- | ---: | --- |
| `2026-06-18` | 2887 | 已更新 |
| `2026-04-30` | 770 | 多数可能是非 CSI300 创业板/数据源映射缺口，后续可按需分批定向修复 |
| `2020-09-25` | 207 | 多数可能是老股票/退市/历史数据源遗留，需要先分类 |
| `1999-12-09` | 2 | 明显异常或极短文件，需要单独核查 |

## CSI300 股票池问题

线上 `instruments/csi300.txt` 当前不是严格 300 只：

- 原始行数：已从 `820` 清理为 `653`
- 大小写归一、去重后唯一代码数：已从 `690` 清理为 `653`
- 重复代码数量：已从 `130` 清理为 `0`

按去重后的实际口径，CSI300 close 文件最新日期分布：

| 最新日期 | 行数 | 说明 |
| --- | ---: | --- |
| `2026-06-18` | 653 | 已更新。该数值按唯一代码统计 |
| `2020-09-25` | 30 | 已确认为退市/已退出股票池的历史污染项 |
| `MISSING` | 7 | instruments 中有代码但 features 中缺 close 文件 |

剩余 `37` 个退市/历史污染项已从当前 `csi300.txt` 中移除。历史行情文件未删除。

## 代表性问题代码

### 已修复的正常上市缺口

这些原先停在 `2026-04-30` 或 `1999-12-09`，Baostock 可取近期日线，已于 2026-06-19 定向执行 `update_cn_data.py --code ... --rebuild-stale`，全部更新到 `2026-06-18`：

`sz300024`, `sz300033`, `sz300058`, `sz300059`, `sz300070`, `sz300072`, `sz300085`, `sz300122`, `sz300124`, `sz300133`, `sz300136`, `sz300142`, `sz300144`, `sz300146`, `sz300168`, `sz300182`, `sz300251`, `sz300296`, `sz300315`, `sz300347`, `sz300408`, `sz300413`, `sz300433`, `sz300498`, `sz300601`, `sz300628`

### 已分类为退市/历史股票池污染

这些多停在 `2020-09-25` 或缺文件，Baostock 基础信息均显示 `status=0` 且带 `out_date`，不适合继续补近期行情。建议后续生成股票池清理候选清单，让你确认后再改 `csi300.txt`：

`sh600068`, `sh600074`, `sh600190`, `sh600220`, `sh600270`, `sh600277`, `sh600297`, `sh600317`, `sh600432`, `sh600485`, `sh600705`, `sh600804`, `sh600811`, `sh600823`, `sh600837`, `sh600978`, `sh601258`, `sh601558`, `sh601989`, `sz000046`, `sz000413`, `sz000540`, `sz000627`, `sz000667`, `sz000671`, `sz000780`, `sz000961`, `sz002411`, `sz002450`, `sz300104`

缺文件但同样属于已退市/历史股票池污染：

`sh600005`, `sh600087`, `sh600102`, `sh600591`, `sh600747`, `sh600849`, `sh601268`

## 已采取的小修

`backend/api/data.py` 的股票健康检查已改为：

- 对 `csi300.txt` 成分股代码做小写归一；
- 统计唯一股票数；
- 额外返回 `raw_total` 和 `duplicate_count`，避免把重复行误认为真实股票数量。

这样页面不会继续把 `820` 误展示成真实唯一成分股数量。实际股票池是否清理、如何清理，仍建议由你确认后再执行。

## 建议下一步

1. 对全市场仍停在 `2026-04-30` 的非 CSI300 股票，可按是否需要全市场覆盖来决定是否继续分批修复。
2. 当前 `csi300.txt` 已按方案 B 清理为 653 个唯一代码，但这仍不是权威“当前 CSI300 300 只”。
3. 中期建议新增 `csi300_current.txt`，用权威成分数据维护真正当前 300 成分股。

## 已执行的定向修复动作

已用 Baostock 做只读可修复性检查，以下 24 个代码均可取到 `2026-05-06` 至 `2026-06-18` 的 32 条日线：

`sz300024`, `sz300033`, `sz300058`, `sz300059`, `sz300070`, `sz300072`, `sz300085`, `sz300122`, `sz300133`, `sz300136`, `sz300142`, `sz300144`, `sz300146`, `sz300168`, `sz300182`, `sz300251`, `sz300296`, `sz300315`, `sz300347`, `sz300408`, `sz300413`, `sz300498`, `sz300601`, `sz300628`

已执行脚本：

```bash
docker cp /tmp/repair_创业板_gap_candidates.sh quant-backend:/tmp/repair_创业板_gap_candidates.sh
docker exec quant-backend bash /tmp/repair_创业板_gap_candidates.sh
```

执行前建议再次确认备份目录存在：

`/home/ubuntu/.qlib/qlib_data/cn_data.bak-before-full-update-20260619-192921`

修复后验证脚本：

```bash
docker cp /tmp/verify_gap_candidate_dates.py quant-backend:/tmp/verify_gap_candidate_dates.py
docker exec quant-backend python /tmp/verify_gap_candidate_dates.py
```

验证结果：24 个代码全部为 `2026-06-18`，`not_latest` 为空。

24 个创业板修复后分布：

| 指标 | 修复前 | 修复后 |
| --- | ---: | ---: |
| 全市场最新到 `2026-06-18` 的 close 文件 | 2861 | 2885 |
| 全市场未最新 close 文件 | 1024 | 1000 |
| CSI300 唯一问题代码 | 63 | 39 |
| CSI300 最新到 `2026-06-18` 的唯一代码 | 627 | 651 |

修复后线上健康接口仍为 `healthy`，`sample_latest_coverage` 从 `0.73` 提升到 `0.74`。

## 剩余 39 个 CSI300 问题代码处理结果

对剩余 39 个问题代码做 Baostock 只读分类后：

- `sz300124` 汇川技术、`sz300433` 蓝思科技仍正常上市，并可取到 `2026-05-06` 至 `2026-06-18` 的近期日线，已定向修复到 `2026-06-18`。
- 其余 37 个均为 `status=0` 且有 `out_date` 的退市/已退出股票池项，不继续补近期行情。

最终 CSI300 唯一代码日期分布：

| 最新日期 | 唯一代码数 | 说明 |
| --- | ---: | --- |
| `2026-06-18` | 653 | 当前清理后股票池全部可用 |

最终全市场 close 文件分布：

| 指标 | 当前值 |
| --- | ---: |
| 最新到 `2026-06-18` 的 close 文件 | 2887 |
| 未最新 close 文件 | 998 |

线上健康接口仍为 `healthy`。

## 已执行 CSI300 股票池方案 B

用户确认后，已执行 `csi300-pool-cleanup-proposal-2026-06-19.md` 中的方案 B：

- 移除 37 个已确认退市/历史污染唯一代码；
- 去除重复行；
- 不删除任何历史行情文件。

线上原文件备份：

`/home/ubuntu/.qlib/qlib_data/cn_data/instruments/csi300.txt.bak-scheme-b-20260619-232110`

执行后：

- `stocks.total = 653`
- `stocks.raw_total = 653`
- `stocks.duplicate_count = 0`
- `/api/data/health = healthy`
