# 研究股票池口径说明：core650（核心研究池）

## 结论（2026-07 本机核验）

| 项目 | 结果 |
|---|---|
| 代码数 | **653 只**（去重后） |
| 板块 | 沪主板 384 / 深主板 237 / 创业板 32（无科创/北交主导） |
| 日线新鲜度 | **99.8%** 最新至 2026-07（651/653 到 07-08） |
| 空心票 | **0** |
| 缺 close | **0** |
| 尾部 ≥30% 跳变 | **1**（可忽略） |
| 真实复权因子 | **649/653** |
| 占全市场 features | **约 14.6%**（4485 只中的核心子集） |

**判定：适合作为「大盘/核心资产研究样本」保留；统计上有效、数据可信。**  
**不可再称为「沪深300成分股」**——官方 CSI300 为约 300 只、市值加权时点成分，与本池不同。

## 命名

| 用途 | ID / 文件 | 展示名 |
|---|---|---|
| 研究股票池（选股/因子/默认回测宇宙） | `core650` → `instruments/core650.txt` | **核心研究池（约650只）** |
| 旧兼容 | `csi300` 自动映射到 `core650`；`csi300.txt` 可仍保留副本 | 兼容旧名 |
| 真实沪深300指数（基准/展示） | `SH000300` / `hs300` | **沪深300指数**（可保留此名） |

## 依赖本池的功能

- 因子分析 / Alpha158 缓存（默认 instruments）
- 回测默认 universe
- 深度学习默认股票列表
- 数据健康统计 / 优先更新
- 均值回归全市场失败时的回退名单

**不依赖本池命名的：** 智能股票池（全市场）、盘后选股（股票池 TopN）、个股行情。

## 维护

```bash
# 若只有旧文件，启动时会自动复制
# instruments/csi300.txt  → instruments/core650.txt
```
